name: frontend_service
description: Front end demo for PSN API Server

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  bloc: ^3.0.0
  equatable: ^1.0.0
  flutter_bloc: ^3.2.0
  flutter_icons: ^1.1.0
  flutter_markdown: ^0.3.5
  http: ^0.12.0+4
  localstorage: ^3.0.2+5

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
